Keep the 7 Report Category cards as static summary KPIs. Their values (Recovery Amount, Active Buckets, Digital Recovery %, Successful Recovery Amount, Running Strategies, SMS Delivery %, Bounce %) must never change when a card is selected. Clicking a card should only apply a global filter to the dashboard KPIs, charts, tables, and reports below. The selected card should only change its visual active state (highlight/border), while all seven card values remain based on the current date range and global filters, not the selected report category.